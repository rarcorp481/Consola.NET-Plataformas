# Catálogo de Cursos (Proyecto de Consola .NET)

Este es un proyecto simple de consola en C# creado como parte del caso práctico de la materia "Plataformas de Colaboración Digital".

La aplicación demuestra un flujo de trabajo básico de desarrollo y gestión de tareas (Trello + Git).

## 🚀 ¿Qué hace la aplicación?

La aplicación ejecuta un "Catálogo de Cursos" directamente en tu terminal.

* Muestra una lista estática de cursos predefinidos.
* Permite al usuario filtrar la lista escribiendo un término de búsqueda (por ejemplo, "Algoritmos" o "Math").
* Utiliza `System.Linq` para realizar el filtrado.

## 📋 Requisitos Previos

Para compilar y ejecutar este proyecto, es necesario tener instalado el **SDK de .NET** (versión 6.0 o superior es recomendada).

Puedes descargarlo desde [el sitio oficial de .NET](https://dotnet.microsoft.com/download).

## ⚡ Cómo Ejecutar el Proyecto

Sigue estos pasos para ejecutar la aplicación:

1. Clona o descarga este repositorio en tu máquina local.
2. Abre tu terminal (como Git Bash, Windows Terminal, o la terminal integrada de VS Code).
3. Navega hasta la carpeta raíz del proyecto (la carpeta donde se encuentra el archivo `CatalogoCursos.csproj`).
4. Ejecuta el siguiente comando:

   ```bash
   dotnet run